import LoginCard from "../layouts/LoginCard";

const Login = () => {
	return (
		<main className="py-24">
			<section
				id="login-section"
				className="px-60"
			>
				<LoginCard />
			</section>
		</main>
	);
};

export default Login;

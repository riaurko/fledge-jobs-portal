import { Fragment } from "react";
import Navbar from "../layouts/Navbar";

const Root = () => {
    return (
        <Fragment>
            <Navbar />
        </Fragment>
    );
};

export default Root;
